export enum InfoNotificationTypeEnum {
  Info = 'info',
  Warning = 'warning',
  Error = 'error',
}

export enum NotificationTypeEnum {
  Ready = 'proof_ready',
  Error = 'artwork_error',
  Changed = 'order_status_changed',
  Awaiting = 'proof_awaiting_approval',
  Dispatched = 'order_dispatched',
}
